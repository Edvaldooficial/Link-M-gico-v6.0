# LinkMágico Chatbot IA — Deploy no Render

## Variáveis de ambiente
```
PORT=10000
ORIGIN=*
NODE_ENV=production
```
## Render
- Build Command: *(em branco)*
- Start Command: `node server.js`
- Node: 18+

## Local
```
npm install
npm start
# http://localhost:10000
```
